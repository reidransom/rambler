# Ramblings

## Dev

    $ npm install bower -g
    $ npm install grunt-cli -g
    $ npm install
    & bower install
    $ ./bin/dev
    $ grunt && grunt watch

## Test

    $ npm test

## Deploy

    $ git push prod master
